<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>{{ title }}</title>
	<link rel="stylesheet" href="{{ '/css/main.css' | url }}" />
</head>
<body>
  <header class="o-box c-header">
		{% include "partials/nav.njk" %}
	</header>
	<main class="o-box"> 
		<article class=" o-centre">
		<h1>{{ title }}</h1>
		{{ content | safe }}
		</article>
	</main>
</body>
</html>